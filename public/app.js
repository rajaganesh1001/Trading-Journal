// The full Trading Journal UI is bootstrapped by the bundled app shell in
// the page HTML, so this standalone module should stay harmless.
// Keeping it as a no-op prevents the older Firebase demo code from crashing
// the site when the page loads.
export {};
